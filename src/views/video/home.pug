extends ../base
include ../mixins/video


block content
      .video-grid1
        each video in videos
          +video(video)

          
        else
          span.empty__message No video's found.
